{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceRoot}/build/app",
            "cwd": "${workspaceRoot}",
            "stopAtEntry": false,
            "valuesFormatting": "parseText"
        }
    ]
}